/*
Fat Model, Skinny Controller, Skinny Views
  -everything operating on data needs to go on model
  -Take out everything of the view
  -View should be stupid, display and do nothing else 
*/

$(function() {TodoApp.start(); });
